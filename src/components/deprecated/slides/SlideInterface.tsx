/**
 * Interface for slide component props
 * Defines the common properties that all slide components should accept
 */
export interface SlideProps {
  // Interface is kept empty but available for future props
}
